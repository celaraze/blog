---
const allArticles = await Astro.glob("../pages/posts/*.md");
---

<ul>
  {
    allArticles.map((article) => (
      <li>
        <a href={article.url}>{article.frontmatter.title}</a>
      </li>
    ))
  }
</ul>
